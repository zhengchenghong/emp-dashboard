import { en } from '@app/language';

export default {
  info: {
    download: {
      message: en['Invalid data has been downloaded!'],
      options: {
        autoHideDuration: 10000,
        variant: 'info'
      }
    }
  }
};
