import err from './errorMessages';
import { en } from '@app/language';

export default {
  success: {
    upload: {
      message: en['The Library file has been uploaded Successfully!'],
      options: {
        autoHideDuration: 10000,
        variant: 'success'
      }
    }
  }
};
